@model System.String
<div class="control-group">
    @Html.Label("", new {@class = "control-label"})
    <div class="controls">
        @Html.TextBox("", Model, new {placeholder = @ViewData["placeholder"]})
        @Html.ValidationMessage("", new { @class = "text-error" })
    </div>
</div>